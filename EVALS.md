# Evals

This repository only defines evaluation placeholders. No model metrics are claimed here.

- `evals/eval_thresholds.yaml` stores target gates for later classifier and answer quality checks.
- `evals/README.md` documents how those thresholds should be interpreted after training artifacts are added.

