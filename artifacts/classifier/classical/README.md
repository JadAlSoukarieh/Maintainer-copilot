# Classical Classifier Artifacts

Expected files in this directory:

- `model.joblib`
- `metrics.json`
- `confusion_matrix.json`
- `test_predictions.jsonl`

These files are copied from Colab training outputs for the classical classifier baseline.

- Produced by: Colab
- Consumed by: local verification, comparison scripts, and later reporting

`model.joblib` and `test_predictions.jsonl` should be treated as artifact files and staged only after Git LFS is configured.

