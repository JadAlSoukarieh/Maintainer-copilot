# Classifier Artifacts

Training artifacts are copied here later from Colab. The model-server already exposes a stable loader interface and placeholder responses so integration can proceed before artifacts exist.

