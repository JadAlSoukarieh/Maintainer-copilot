# Processed Dataset Files

Expected files in this directory:

- `train.jsonl`
- `val.jsonl`
- `test.jsonl`
- `excluded_issues.jsonl`
- `label_mapping.json`
- `data_hash.txt`
- `split_manifest.json`

These files come from dataset preparation and split generation. They may initially be copied from Colab and later regenerated by VS Code preprocessing scripts if the local workflow expands.

- Produced by: Colab now, potentially VS Code preprocessing scripts later
- Consumed by: training, artifact verification, and later evaluation scripts

`data_hash.txt` should record the dataset hash associated with the processed split set.

